name: Deploy

on:
  push:
    branches: [master]

jobs:
   build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]
  
    steps:
      # - uses: actions/checkout@v2
      - name: Checkout
        uses: actions/checkout@v2

      - name: Docker Hub Login
        run: echo '${{ secrets.DOCKER_PASSWORD }}' | docker login -u '${{ secrets.DOCKER_ID }}' --password-stdin
  
      - name: Test
        run: |
          docker build -t meirthing02/react-test -f ./client/Dockerfile.dev ./client
          docker run meirthing02/react-test npm test -- --coverage --watchAll=false

      - name: Build Docker Images
        run: |
          docker build -t meirthing02/multi-client ./client
          docker build -t meirthing02/multi-nginx ./nginx
          docker build -t meirthing02/multi-server ./server
          docker build -t meirthing02/multi-worker ./worker
          docker push meirthing02/multi-client
          docker push meirthing02/multi-nginx
          docker push meirthing02/multi-server
          docker push meirthing02/multi-worker


